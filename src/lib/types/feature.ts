export interface Feature {
  id: string;
  name: string;
  created_at: string;
}

export interface CarFeature {
  id: string;
  name: string;
  available: boolean;
}

export interface PrivateListingFeature {
  id: string;
  name: string;
  available: boolean;
}